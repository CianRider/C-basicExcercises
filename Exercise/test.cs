using System;
using System.Collections.Generic;
using System.Text;

public class Class1
{
    static void Main(string[] args)
    {
        var inv = new List<int>();
        long order = 4;

        inv.Add(2);
        inv.Add(5);

        int profit = 0;
        for (var i = order; i < 0; i--)
        {
            var max = inv.Rep
            Console.WriteLine(max);
            profit = profit + max;
            Console.WriteLine(profit);
            //inventory.Replace(max, max--);
        }
    }
}
